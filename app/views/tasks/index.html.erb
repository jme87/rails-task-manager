<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>
      <% if task.completed%>
      <p>✅</p>
      <% else %>
        <p>🗒️</p>
      <% end %>
    </li>
  <% end %>
</ul>

<%= link_to "➕ Add new task", new_task_path %>
