<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "✍️ Edit", edit_task_path %>
<br>
<%= link_to "❌ Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this task?"}
%>
<br>
<%= link_to "↩ Back to task list", tasks_path(@tasks) %>
